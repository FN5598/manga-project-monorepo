info:
  name: Upload Manga
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/manga/upload-chapter"
  body:
    type: json
    data: |-
      {
        "mangaData": {
          "title": "Piecer",
          "author": "Piercer",
          "description": "hello world world hello",
          "status": "ongoing",
          "genres": {}
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
