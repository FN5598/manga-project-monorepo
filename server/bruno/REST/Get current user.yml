info:
  name: Get current user
  type: http
  seq: 10

http:
  method: GET
  url: "{{BASE_URL}}/user/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
