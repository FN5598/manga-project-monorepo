info:
  name: Get current user
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/api/users/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
