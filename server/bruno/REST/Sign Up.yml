info:
  name: Sign Up
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/sign-up"
  body:
    type: json
    data: |-
      {
        "username": "Pierce",
        "email": "pierce@cross.ua.com",
        "password": "pierce"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
