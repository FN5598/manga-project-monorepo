info:
  name: Logout
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/logout"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
