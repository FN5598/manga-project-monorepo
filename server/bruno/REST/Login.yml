info:
  name: Login
  type: http
  seq: 8

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "password": "123123123",
        "email": "bobross@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
